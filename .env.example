# Copy this file to .env and fill in real values.
# .env is gitignored — NEVER commit real keys.

# Add API keys / credentials for dataset sources here as they are
# introduced (e.g. Zenodo, Figshare, NCBI, data portals).
# Example:
# ZENODO_TOKEN=your-zenodo-token-here
